{
  "name": "tests",
  "version": "1.0.0",
  "description": "Azure Pipelines task extension for running Pulumi apps.",
  "main": "_suite.js",
  "scripts": {
    "test": "tsc -p ../ && tsc && mocha _suite.js",
    "lint": "tslint -c tslint.json -p ."
  },
  "author": "Pulumi",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.3.1",
    "tslint": "^6.1.3",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "mocha": "^11.6.0"
  }
}
